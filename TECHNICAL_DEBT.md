# 技术债记录

## 已锁定业绩结算的历史重放（已解决）

处理日期：2026-08-10。

结算算法现已正式版本化：v1 固定保留原始的成员汇总收费规则，v2 使用入金批次独立核算规则，v3 在逐批核算基础上将计费周期与每份份额的高水位 NAV 分离。新结算明确保存 `algorithmVersion: 3`，历史回放按每笔记录自己的版本调用冻结算法，不再统一调用最新版本。

无版本旧记录会在首次加载或备份导入时执行安全迁移：系统分别使用受支持的历史算法重放，并与锁定快照精确比较后补充版本；无法唯一确认或快照不一致的有效结算会拒绝加载/导入，不会静默改写余额。已冲销结算因历史账期可能在解锁后被修改，使用其不可变快照明细格式识别版本，且不参与有效余额回放。

备份恢复会在内存中合并普通账本与有效结算进行完整校验；`db.json`、`config.json` 和 `settlements.json` 使用持久化事务日志提交，普通异常会立即回滚，进程崩溃或断电则在下次启动时恢复完整旧版本。所有核心数据修改都会提前生成可直接导入的三文件 ZIP 快照并滚动保留 15 份；初始化后的结算账本若丢失会失败关闭，不会创建空账本。

核心计算器拒绝任何缺少 `algorithmVersion` 的结算事件，旧记录只能通过快照校验迁移器进入回放。自动化测试覆盖 v1/v2 迁移、v3 计费周期与高水位 NAV、快照冲突拒绝、v1 撤销后同日 v3 重结算及导出导入往返、结算文件缺失保护、普通提交失败回滚和进程中断后的事务日志恢复。
